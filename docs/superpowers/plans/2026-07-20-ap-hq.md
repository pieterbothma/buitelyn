# AP HQ Implementation Plan (M0 ✅ → M6)

> Spec: `docs/superpowers/specs/2026-07-20-ap-hq-admin-design.md` (read it first — data model + locked decisions live there). Execute milestone-by-milestone; commit + real-browser verify each.

**State after M0 (2026-07-20):** `admin/` Next.js app live at **ap-hq.vercel.app** (Vercel project `ap-hq`, SSO protection off — app has its own auth). Supabase project **ap-hq** `mstrumkcyfikbddfmjti` (eu-west-1): full schema + RLS + storage buckets pushed (`admin/supabase/migrations/`), 5 workspaces + recurring rules seeded. Magic-link auth works; **`app_config.allowlisted_email` is still `CHANGE_ME@example.com`** — set to AP's email (Piet's for testing) or every query returns empty. Env in `admin/.env.local` + Vercel prod env (incl. service key + db password; db push via `aws-0-eu-west-1.pooler.supabase.com` — direct IPv6 fails on this network). Supabase mgmt token: macOS keychain `Supabase CLI` (strip `go-keyring-base64:`, base64 -d).

## M1 — Pyplyn

- `lib/cards.ts`: `generateCardsForDate(date)` — for each aktief rule where `date`'s ISO weekday ∈ `weekdae`, upsert card (unique `rule_id+due_at`), titel = rule.naam, due_at = date+tyd (Africa/Johannesburg!). Vitest: weekday filtering, idempotency, TZ.
- `/api/cron/generate-cards` (service role): today + 7 days ahead; also mark `beplan/produksie` cards older than 24h as `gemis`. `vercel.json` cron daily 03:00 UTC (05:00 SAST).
- `/w/[slug]/pyplyn`: board with status columns, click-through status advance (server actions), overdue red. Week list view.
- Wire Vandag (already queries cards) — verify with real cards.

## M2 — Idees

- Global quick-add: floating `+` in Shell → dialog (titel, nota, skakel, workspace?, format?). Server action insert.
- `/w/[slug]/idees` list + `/inkassie` (workspace_id null) with tag-to-workspace buttons.
- "Maak kaart" promote: creates `cards` row (due date picker), links `idea_id`.

## M3 — Kliënte & Fakture

- `lib/invoices.ts`: `nextInvoiceNumber(workspaceId)` — `invoice_counters` upsert w/ year rollover → `PRE-2026-007`. Vitest.
- Clients CRUD (`/w/[slug]/kliente`). Invoices: form (client, lines, datum), totals in sent (bigint), ZAR format `R 5 000,00`.
- PDF: `@react-pdf/renderer` route `/api/invoices/[id]/pdf` — entity-branded (workspace naam + accent, League Spartan, paper/ink), bank details from `entity_settings`; store copy in `invoice-pdfs` bucket.
- Send: `/api/invoices/[id]/send` — Resend (from `entity_settings.faktuur_epos_from`; NEEDS AP domain in Resend), PDF attached, status→gestuur. Mark-betaal button.

## M4 — Stories (Promenader)

- `/w/promenader/stories`: list + detail; status stepper; file upload to `stories` bucket (signed URLs), `story_files` rows.

## M5 — Nuusbrief → Oudio

- Port `web/lib/feed.ts` parser into `admin/lib/feed.ts` BUT keep full `content:encoded` text (strip to readable plain text for TTS).
- `/w/buitelyn/oudio`: post list → editable textarea → POST `/api/audio/generate`: ElevenLabs TTS (env `ELEVENLABS_API_KEY` — Piet has from nuuspod; `VOICE_ID` env, AP to choose), chunk long text, concat MP3, store in `audio-episodes` bucket, row in `audio_episodes`, download button + history.

## M6 — Telegram-agent

- BotFather bot (AP or Piet creates; env `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ALLOWED_USER_ID`, webhook secret path). `setWebhook` → `/api/telegram/[secret]`.
- Voice: getFile → download OGG → Gemini transcribe (env `GEMINI_API_KEY`, af hint) → text. Store OGG in `voicenotes`.
- Agent: Claude API (`claude-sonnet-5`) tool loop; tools = `stoor_idee`, `skep_faktuur_konsep`, `lys_vandag`, `lys_onbetaal`, `merk_gepubliseer` calling same lib functions as UI (service role client).
- Idea filing: reply inline keyboard (5 workspaces + Inkassie); callback_query updates `ideas.workspace_id`.
- Invoice draft: agent replies summary + buttons `Stuur / Wysig / Skrap`; pending action in `telegram_state`; Stuur calls the M3 send route. NEVER auto-send.
- All routes verify secret path + from.id === allowlisted user.

## Open AP items (tracked in spec §Open items)

Allowlist email · bank details + VAT · Resend domain · ElevenLabs voice · Telegram /start · Ei-Aai + Tweede Trump times · AFNA Skool URL.
